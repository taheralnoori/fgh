{
  "name": "doc_to_pdf",
  "description": "pdf to docx",
  "env": {
    "TELEGRAM_TOKEN": {
      "description": "Get this value from my.telegram.org.",
      "required": true
    },
    "CONVERTAPI_SECRET": {
      "description": "Get this value from my.telegram.org.",
      "required": true
    }
  },
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "free"
    }
  }
}
